import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv("https://raw.githubusercontent.com/fivethirtyeight/data/master/alcohol-consumption/drinks.csv")

top10 = df.sort_values(by="total_litres_of_pure_alcohol", ascending=False).head(10)
print(top10[["country", "total_litres_of_pure_alcohol"]])

media_vino = df["wine_servings"].mean()
media_birra = df["beer_servings"].mean()
media_distillati = df["spirit_servings"].mean()

print(f"Media vino: {media_vino}")
print(f"Media birra: {media_birra}")
print(f"Media distillati: {media_distillati}")

df["alcohol_index"] = (df["beer_servings"] + df["wine_servings"] + df["spirit_servings"]) / 3
indice_max = df["alcohol_index"].idxmax()
paese_max = df.loc[indice_max]

print("Paese con alcohol_index massimo:", paese_max["country"])

birre_100 = df[df["beer_servings"] > 100]
print(birre_100[["country", "beer_servings"]])

plt.figure(figsize=(10, 5))
plt.bar(top10["country"], top10["total_litres_of_pure_alcohol"])
plt.title("Top 10 paesi per litri di alcool puro")
plt.xlabel("Paese")
plt.ylabel("Litri di alcool puro")
plt.xticks(rotation=45, ha="right")
plt.tight_layout()
plt.show()

df_sorted = df.sort_values(by="country").reset_index(drop=True)

plt.figure(figsize=(12, 5))
plt.plot(df_sorted.index, df_sorted["wine_servings"])
plt.title("Distribuzione consumo di vino (Paesi in ordine alfabetico)")
plt.xlabel("Indice Paese (Ordinati A-Z)")
plt.ylabel("Porzioni di vino")
plt.tight_layout()
plt.show()
