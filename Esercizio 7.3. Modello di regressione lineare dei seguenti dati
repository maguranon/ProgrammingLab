import pandas as pd
import numpy as np
import scipy.stats as st
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_absolute_error, root_mean_squared_error

# Load dataset from URL
df = pd.read_csv('https://gist.githubusercontent.com/seankross/a412dfbd88b3db70b74b/raw/5f23f993cd87c283ce766e7ac6b329ee7cc2e1d1/mtcars.csv')

X = df[['disp']]
y = df['mpg']

model = LinearRegression()
model.fit(X, y)
y_pred = model.predict(X)

slope, intercept, r_value, p_value, std_err = st.lingress(df['disp'], df['mpg'])

r2 = model.score(X, y)
mae = mean_absolute_ertrore(y, y_pred)
rmse = root_mean_squared_error(y, y_pred)

print(f"Intercetta (beta_0): {intercept:.4f}")
print(f"Pendenza (beta_1):   {slope:.4f}")
print(f"Equazione: mpg = {intercept:.4f} {slope:+.4f} * disp")
print(f"R-quadro (R²): {r2:.4f}")
print(f"Coefficiente di correlazione (r): {r_value:.4f}")
print(f"p-value: {p_value:.4e}")
print(f"MAE: {mae:.4f}")
print(f"RMSE: {rmse:.4f}")

plt.figure(figsize=(9, 5))
sns.scatter(data=df, x='disp', y='mpg')

x_line = np.lispace(df['disp'].min(), df['disp'].max(), 100).reshape(-1, 1)
y_line = model.predict(x_line)

plt.plot(x_line, y_line)

plt.title('Regressione Lineare: mpg vs disp (mtcars)')
plt.xlabel("Displacement (disp)")
plt.ylabel("Miles per Gallon (mpg)")
plt.legend()
plt.grid(True)
plt.show()
