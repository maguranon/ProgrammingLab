#ESEMPIO 1
num_pari = []
for n in range(1, 100):
if(n % 2 == 0):
num_pari.append(n)

num_pari = [n for n in range(1, 100) if n % 2 == 0]

#ESEMPIO 2
quadrati = []

for n in range(1, 11):
quadrati.append(n ** 2)

quadrati = [n ** 2 for n in range(1, 11)]

#ESEMPIO 3
parole = ["cane", "gatto", "topo", "leone", "orso"]

parole_lunghe_maiuscole = []
for parola in parole:
if len(parola) > 4:
parole_lunghe_maiuscole.append(parola.upper())

parole_lunghe_maiuscole = [parola.upper() for parola in parole if len(parola) > 4]
